import ToolbarItem from "./toolbar.item";

const commonItems = [
    new ToolbarItem('Главная', '/href')
];

const hrItems = [
    new ToolbarItem('Кандидаты', '/candidates')
]

const candidateItems = [
    new ToolbarItem('Моё резюме', '/myResume')
]